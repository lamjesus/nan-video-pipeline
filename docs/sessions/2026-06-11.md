# Sesión 2026-06-11 — Rescate del template de render + guion validado (Tarea D)

Bitácora de lo cambiado en esta PR y por qué. Memoria para el equipo.

## Qué se hizo

- **Rescate de la PR #4.** La PR #4 (template HTML + GSAP + captions) se mergeó
  contra `feat/compose-manifest-and-workspace` 21 segundos **después** de que esa
  rama entrara en `main` vía PR #3 → `src/render/` nunca llegó a `main`. Esta
  rama parte de la rama original de adri, le mergea `main` (el merge trajo la
  etapa de subtítulos, que faltaba allí) y reconcilia el `yarn.lock`. Moraleja
  para PRs apiladas: re-apuntar la base a `main` antes de mergear.
- **Tarea D — Guion con qwen3.6.** `src/pipeline/script-util.ts` (lógica pura):
  `extractJson` tolera bloques `<think>`, vallas markdown y prosa alrededor;
  `validateStoryboard` exige 10 escenas, campos requeridos y tiempos coherentes,
  con errores que nombran el campo (`scenes[3].voiceover`) para usarlos como
  feedback. `01-script.ts`: retry con ese feedback (3 intentos), `reasoning_config`
  desactivado con degradación si el cluster lo rechazara, slug opcional
  (`yarn script "<tema>" [slug]`), `createNanCall` y errores ERROR/WHY/FIX.
- **Casos generados sin registro manual.** `load.ts` hace fallback a un import
  dinámico (`src/content/<slug>.ts`) con el formato del slug validado; `CASES`
  queda para los casos curados. Se commitea `caso-alejandria` como segundo caso
  de ejemplo cargable.
- **Fix Windows en `load.ts`.** El guard de auto-ejecución comparaba
  `import.meta.url` con `file://${argv[1]}`, que en Windows nunca coincide
  (backslashes) → `yarn load` salía en silencio sin probar nada. Ahora usa
  `pathToFileURL`.

## Verificación

- `yarn typecheck` ✅ · `yarn test` ✅ (14 tests nuevos de `script-util`).
- **E2E real contra el cluster:** `yarn script "La biblioteca de Alejandría"
  caso-alejandria` → Storyboard válido al primer intento (10 escenas, 45s,
  ~26s de generación). El cluster **acepta `reasoning_config`** (sin warning).
- `yarn load caso-alejandria` carga por fallback; `yarn load caso-inexistente`
  falla con mensaje claro y pista de generación.

## Hallazgos

- PRs apiladas: si la base se mergea primero, GitHub no re-apunta la PR hija a
  `main` automáticamente → revisar `baseRefName` antes de mergear.
- `yarn load` era un no-op en Windows (guard de `import.meta.url`).
- `caso-ejemplo` tiene 9 escenas; la validación de 10 aplica solo a casos
  generados nuevos, no al cargador.

## Segunda parte de la sesión — pipeline ejecutable de punta a punta

- **Tarea A (voz) verificada:** el "stub" de `03-voice.ts` funcionaba tal cual —
  kokoro respondió a la primera (`caso-ejemplo.mp3`, estéreo, 58.5s). Nadie lo
  había ejecutado nunca. Queda elegir voz (em_alex vs ef_dora).
- **Compose materializa el workspace** (`compose-util.ts`, puro + testeado):
  copia `images/`, `captions/` y `audio/` dentro de `render-<slug>/` — las rutas
  relativas del HTML ya resuelven; autocontenido para HyperFrames.
- **Reescalado de tiempos:** el guion inventa los tiempos (90s) pero el audio
  real manda (58.5s); `rescaleScenesToAudio` escala las escenas a la duración
  medida con ffprobe. Sin cola muda al final del video.
- **`preview.html`** (`src/render/preview.ts`): la misma composición pero
  reproducible por humanos con doble click — audio + botón de play + captions
  incrustados (file:// bloquea fetch). `index.html` queda intacto como fuente
  de frames determinista para el runner.
- **Subtítulos estilo CapCut:** `chunkSegments` trocea los párrafos en bloques
  de ≤42 caracteres que se refrescan con la voz (reparto de tiempo proporcional
  dentro de cada segmento alineado).
- Verificación: typecheck ✅ · 132/132 tests ✅ · `yarn compose caso-ejemplo`
  regenera el workspace completo sin pasos manuales.

## Tercera parte — auditoría completa, casos como datos y batería

- **Auditoría** (revisor independiente sobre la rama): 4 P1, 5 P2, 5 P3 — todos
  resueltos salvo el orquestador (Tarea F, de Luis). Destacados: el semáforo de
  `nan-call` creaba una instancia por llamada y **no limitaba nada** → ahora es
  global al proceso (máx 3 en vuelo, **límite duro del cluster**; casos en máx
  2 carriles paralelos); la alineación de subtítulos colocaba las palabras sin
  match al final de su escena → reescrita en orden canónico con ventanas entre
  vecinos (4 tests de regresión).
- **Casos como datos:** `src/content/*.ts` → `content/<slug>.yml` (YAML
  editable con comentarios; el cargador parsea + valida con
  `validateStoryboard`; nada generado se ejecuta). El manifest sigue en JSON
  como contrato máquina-a-máquina, ahora con **rutas relativas al workspace**
  (portable para el runner).
- **Reorganización:** workspaces agrupados en `renders/<slug>/`; imágenes por
  caso en `assets/images/<slug>/` (los scene-id colisionaban entre casos).
- **Parametrizable:** escenas por caso (`yarn script "<tema>" <slug> 12`,
  3-20) y candidatas por proveedor (`config.yml` → `media.candidates: 8`).
  Captions estilo CapCut con contraste alto; preview en 9:16 real; GSAP con SRI.
- **Batería de 11 casos** (máx 2 carriles, uno a uno dentro de cada carril):
  **11/11 completos** — Pompeya (9 escenas), Alejandría, Redes, Titanic,
  Muralla, GPS, Numancia, Tulipanes, Milgram, Mary Celeste y Cuántica (10
  escenas cada uno). Todas las escenas con imagen (la etapa falla en alto si
  no), subtítulos cortos (18-35 bloques/caso) y `renders/<slug>/preview.html`
  reproducible con doble click. Único fallo: un crash transitorio de la voz
  (`0xC0000409`, 1 de 12 ejecuciones; reintento OK — ver TROUBLESHOOTING).
  Los 11 recompuestos al cierre con el template final (captions CapCut de alto
  contraste, encuadre 9:16, GSAP con SRI, manifest portable).

## Checkpoint para retomar (pausa 2026-06-11 por la tarde)

**Hecho y pusheado:** rama `feat/script-validation-and-render-template` con el
rescate de la PR #4 + Tarea D + fixes de auditoría (11/12) + casos en YAML +
11 casos verificados e2e. 147 tests, typecheck limpio. **La PR NO está abierta**
(pendiente del OK de Manu).

**Decisiones pendientes (en orden):**
1. ~~Elegir los 3 casos golden~~ **ELEGIDOS (Manu):** `caso-ejemplo` (9,
   curado, histórico) + `caso-redes` (10, técnico) + `caso-numancia`
   **regenerado con 12 escenas** (España histórica). `content/` podado a los
   3; fichas en `docs/casos-uso/`. Los otros 8 casos de la batería quedan como
   evidencia local regenerable.
2. **Verificación de Manu:** abrir 2-3 `renders/<slug>/preview.html` y dar OK.
3. **Abrir la PR** (texto redactado) y avisar a adri de las extensiones en su
   zona (compose/render/subtítulos) y los hallazgos de su template.
4. Elegir voz por defecto (`em_alex` vs `ef_dora`) — decisión de oído.

## Siguiente

- **Solo quedan dos piezas de producto:** el runner de HyperFrames (HTML → MP4
  vertical; anunciado por adri) y el orquestador real (`yarn produce` encadena
  hoy solo 1-3; Tarea F, Luis) — más el merge de esta rama.
- Para adri (hallazgos en su template): el CSS genera `.container` 9:16 que el
  HTML no usa (las escenas cuelgan de `body`); el `<script>` de GSAP por CDN va
  sin SRI (`integrity=`).
- Tarea I (imágenes generadas al pool por GUI) y Tarea E (biblioteca, huérfana).
  Mejorar search terms de visión con qwen3.6 (los temas técnicos, ej. IA, tienen
  candidatas débiles en Wikimedia).
